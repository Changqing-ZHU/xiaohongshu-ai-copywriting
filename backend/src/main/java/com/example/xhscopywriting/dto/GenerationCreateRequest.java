package com.example.xhscopywriting.dto;

/**
 * The MVP creation endpoint currently has no input fields. This DTO keeps the
 * HTTP contract separate from the database model for later, explicit growth.
 */
public record GenerationCreateRequest() {
}
